You are a QA Engineer reviewing the following Playwright test.

CONTEXT:
- This is a GENERIC MODE test - no specific Page Objects required
- Use standard Playwright best practices
- Focus on robust selectors and clear test logic

TEST FILE: Verify_WhatsApp_Contact_Link_Functionality.test.ts
PATH: C:\My_VSC_Projects\justVSC\playwright-automation-framework\tests\generated-from-agentFallBack\Verify_WhatsApp_Contact_Link_Functionality.test.ts

CODE:
/**
 * 🤖 AI AGENT HANDOVER NOTE
 * CONTEXT:
 * - Page ID: manual
 * - Jira Key: N/A
 * - Report: agent/generation_report_manual.json
 * - Mode: Generic (Universal) with Page Objects
 * 
 * 🎬 HEADED MODE: This test will run with visible browser
 * Run with: npx playwright test Verify_WhatsApp_Contact_Link_Functionality.test.ts --headed
 */

import { test, expect } from '@playwright/test';
import { LandingPage } from '../../page-objects/LandingPage';
import { handlePageInterruptions } from '../../utils/pageInterruptions';

const inputDataProviders = [
  {
    "caseName": "case_1",
    "phone": "876586758"
  },
  {
    "caseName": "case_2",
    "phone": "876586759"
  },
  {
    "caseName": "case_3",
    "phone": "876586757"
  },
  {
    "caseName": "case_4",
    "phone": "8765867580"
  },
  {
    "caseName": "case_5",
    "phone": "999999"
  }
];


test.describe('Verify_WhatsApp_Contact_Link_Functionality', () => {
  
  test.beforeEach(async () => {
      test.info().annotations.push({ type: 'Owner', description: 'AI Agent' });
      test.info().annotations.push({ type: 'Test Type', description: 'Generic' });
      test.info().annotations.push({ type: 'Description', description: 'Verify WhatsApp Contact Link Functionality' });
  });

  test('Verify WhatsApp Contact Link Functionality', { tag: ['@smoke', '@generic'] }, async ({ page }) => {
    // Initialize page object
    const landingPage = new LandingPage(page);
    const inputData = inputDataProviders[0];
    // =========================================
    // STEP 1: NAVIGATE TO PAGE: HTTPS://SOLO.BEATWISH.LIVE/
    // =========================================
    console.log('\n🚀 STEP 1: Navigate to page: https://solo.beatwish.live/');

    console.log('📍 Navigating to URL: https://solo.beatwish.live/');
    await page.goto('https://solo.beatwish.live/', { waitUntil: 'domcontentloaded', timeout: 60000 });
    await expect(page.locator('body')).toBeVisible({ timeout: 15000 });
    await handlePageInterruptions(page);
    console.log('✅ Navigation completed successfully');
    console.log('✅ Step 1 completed');

    // =========================================
    // STEP 2: LOCATE AND CLICK ON A VISIBLE WHATSAPP CONTACT LINK (E.G., THE ONE WITH 'ARIA-LABEL="שליחת הודעה בוואטסאפ"' OR 'HREF="HTTPS://WA.ME/972508266273"').
    // =========================================
    console.log('\n🚀 STEP 2: Locate and click on a visible WhatsApp contact link (e.g., the one with 'aria-label="שליחת הודעה בוואטסאפ"' or 'href="https://wa.me/972508266273"').');

    await handlePageInterruptions(page);

    console.log('🖱️ Preparing to click element: a visible WhatsApp contact link (e.g., the one with \'aria-label="שליחת הודעה בוואטסאפ"\' or \'href="https://wa.me/972508266273"\').');
    console.log('⚠️ Generic click action for: a visible WhatsApp contact link (e.g., the one with \'aria-label="שליחת הודעה בוואטסאפ"\' or \'href="https://wa.me/972508266273"\').');
    // Generic click action - add custom method as needed
    const element2 = page.getByText('a visible WhatsApp contact link (e.g., the one with \'aria-label="שליחת הודעה בוואטסאפ"\' or \'href="https://wa.me/972508266273"\').', { exact: false }).first();
    await expect(element2).toBeVisible({ timeout: 10000 });
    await element2.click();
    console.log('✅ Clicked element: a visible WhatsApp contact link (e.g., the one with \'aria-label="שליחת הודעה בוואטסאפ"\' or \'href="https://wa.me/972508266273"\').');
    console.log('✅ Step 2 completed');

    // Verification: The browser should successfully initiate a WhatsApp chat with the specified phone number ('972508266273').
    // Verification: The URL should change to 'https://wa.me/972508266273' or trigger the WhatsApp application/web client, indicating a successful redirection.
    // Verification: Reason: WhatsApp is a primary and direct communication channel for potential customers to inquire about services. A non-functional or incorrect WhatsApp link directly hinders lead generation and customer engagement, impacting business significantly.
    await page.waitForLoadState('networkidle');
    const currentUrl = page.url();
    expect(currentUrl).not.toBe('https://www.calm.com/'); // Should have redirected

  });
});

export {}; // Make this a module


TASKS:
1. Review the test for clarity and correctness
2. Improve selectors to be more robust
3. Add proper waits and assertions
4. Ensure test follows Playwright best practices
5. Preserve or add the page interruption handler after navigation and before major scenario actions
6. Add any missing error handling
7. Return the complete, improved test code

GENERIC MODE GUIDELINES:
- Prefer semantic selectors: getByRole, getByLabel, getByText
- Use data-testid attributes when available
- Add proper waits for dynamic content
- Include meaningful assertions
- Structure tests for readability and maintenance
- Keep `handlePageInterruptions(page)` for cookie banners, privacy prompts, announcement modals, newsletter popups, overlays, and interstitials
- If `inputDataProviders` exists in the test, preserve it and use those values when filling form/input fields
- Do not require `main` or `[role="main"]` for generic page-load verification unless the snapshot proves it exists. Prefer `body` plus visible content such as headings, nav, article, section, or body.

UNIVERSAL SELECTORS TO USE:
{
  "login": {
    "username": [
      "input[type='text']",
      "input[name='username']",
      "input[placeholder*='username']",
      "#username"
    ],
    "password": [
      "input[type='password']",
      "input[name='password']",
      "input[placeholder*='password']",
      "#password"
    ],
    "submit": [
      "button[type='submit']",
      "input[type='submit']",
      "button:has-text('Login')",
      ".login-button"
    ]
  },
  "navigation": {
    "menu": [
      "nav",
      ".nav",
      "[role='navigation']",
      ".menu"
    ],
    "links": [
      "a[href]",
      "button:has-text('')",
      ".link"
    ],
    "dropdown": [
      "select",
      ".dropdown",
      "[role='combobox']"
    ]
  },
  "common": {
    "button": [
      "button",
      "input[type='button']",
      ".btn",
      "[role='button']"
    ],
    "input": [
      "input",
      "textarea",
      "[contenteditable='true']"
    ],
    "text": [
      "p",
      "span",
      "div",
      "h1, h2, h3, h4, h5, h6"
    ],
    "form": [
      "form",
      ".form",
      "[role='form']"
    ]
  }
}

Return only the complete TypeScript test code wrapped in triple backticks.
